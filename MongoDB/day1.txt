- show dbs 

- db (current database)

- use database (switch to database if it exist and create new if it is not exist)

- show collections (see tables inside database)

- db.createCollection('students')

- db.collectionname.insertOne({name:'ali',email:'ali@example.com',phone:123456,grades:[1,2,3,4,5],adress:{city:'cairo',area:'Maadi'})


-Mongodb create id automatically 


- db.collectionName.insertMany([{}]); (will create automatically colectionname if isnot exist and insert inside it document )

- db.collectionname.find() (show our records or document)

- collections is array of documents 

- db.collectionname.update(query)
- db.students.update({name:"Ali"},{email:"ali@ali.com}) // will replace all document with new value 
so it's recommended to use updateOne() ,updateMany() instead update to replace one value or many slected values 

- db.collectionname.updateOne({name:"Ali"},{$set:{email:"ali@ali.com}}) // if we set attribut not defined then mongo will create it automatically 

- db.collectionname.updateMany({},{}) //update all values 


- db.students.deleteMany({}) //remove all documents 
- db.students.drop() //remove collection from db 
- db.dropDatabase() //remove database 