export declare class Course {
    id: number;
    description: string;
    name: string;
    maxScore: number;
    constructor(id: number, description: string, name: string, maxScore: number);
}
